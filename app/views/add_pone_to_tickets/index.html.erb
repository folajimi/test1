<h1>Listing add_pone_to_tickets</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @add_pone_to_tickets.each do |add_pone_to_ticket| %>
  <tr>
    <td><%= link_to 'Show', add_pone_to_ticket %></td>
    <td><%= link_to 'Edit', edit_add_pone_to_ticket_path(add_pone_to_ticket) %></td>
    <td><%= link_to 'Destroy', add_pone_to_ticket, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Add pone to ticket', new_add_pone_to_ticket_path %>
