<%= form_for(@add_pone_to_ticket) do |f| %>
  <% if @add_pone_to_ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@add_pone_to_ticket.errors.count, "error") %> prohibited this add_pone_to_ticket from being saved:</h2>

      <ul>
      <% @add_pone_to_ticket.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
